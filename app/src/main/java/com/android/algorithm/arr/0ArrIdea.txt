数组特点：

1、内存空间连续
2、数组size是固定
3、通过索引访问
4、存储相同类型，所以空间一样大
5、边界检查

ArryaList动态数组（数组列表）：
长度可变


结题思路：
1、是否排序
2、是否有重复
3、是否有负数

4、因为通过索引访问，通过下标分组，分治法处理：例如
    二分法、
    分组、
    步长分组等

5、删除：不要使用固有思维：删除一个省下的移动。而是把需要保留的排在前面就可以。

6、数组初始化
  //动态数组  的标准分配
  int[] arrayA = new int[5];

  //静态数组  标准分配
 int[] arr = {1, 2, 3, 3, 5, 6};
 char[] arrayB = new char[]{'大', '来', '嘞'};

7.寻找子数组　：　滑动窗口思想
　　　寻找和为１０的子数组。
　　　　